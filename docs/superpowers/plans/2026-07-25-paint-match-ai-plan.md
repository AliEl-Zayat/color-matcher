# Paint Match AI — Implementation Plan

1. Configure Vite, Tailwind v4, path aliases, PWA manifest/icons.
2. Define Zod schemas + TypeScript types for palettes, paints, mixes, projects.
3. Implement Dexie DB, seed palettes, TanStack Query hooks.
4. Implement color utilities + DeltaE2000 mixer + paint calculator.
5. Build shadcn-style UI primitives and app shell with bottom nav.
6. Ship Match, Live, Palettes, History, Projects pages.
7. Add export (PNG/PDF), settings, future AI stubs.
8. Verify production build; commit.
